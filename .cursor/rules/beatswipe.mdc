---
description: BeatSwipe core context — product, deploy, pitfalls, current status (always on)
alwaysApply: true
---

# BeatSwipe — Core Rules

Producer swipe portfolio for Instagram bio links. Fans swipe beats, save Favorites, buy via producer store links. BeatSwipe takes no cut. Luca = PO, deployer, user #1.

**More detail:** `@beatswipe-archive.mdc` (history, code maps, long changelogs — only when needed).

## Marketing

- **Never** say „Tinder" (brand)
- **Brand URL immer** `beatswipe.app` — nie nur „BeatSwipe" ohne TLD (Verwechslung mit **beatswipe.net**)
- Slogan: **Your bio link. Their next beat.**
- Product: „Your swipe page for the bio" / „Free swipe portfolio for producers"
- **Konkurrenz:** `beatswipe.net` live seit 25.06.2026 — TikTok-Feed, free downloads, collab; **anderes Produkt** als unsere Producer-`/p/`-Pages. Kontakt dort: `legal@beatswipe.com`. Wir: AT/EU, `hellobeatswipe@gmail.com`, `@beatswipe.app`

## Product (Phase 1)

- **Core:** `beatswipe.app/p/producername` — full-screen swipe portfolio (not a beat list profile)
- **Home:** Landing + tool marketing; Discover/Favorites exist but invite-gated
- **Onboarding:** Invite code from Luca (DM) — gated: Discover, Favorites, My Page, Profile. Open: Landing, `/p/*`
- **Monetization:** none now (student). No cut on beat sales.
- **Mobile-first** (Instagram traffic). `/p/` keeps beat glow, not app purple wash.

## Code layout

| File | Role |
|------|------|
| `index.html` | HTML + small init block |
| `app.js` | Discover, auth, favorites, profile, nav, swipe, toast |
| `mypage.js` | My Page upload, dashboard, beat edit/order |
| `portfolio.js` | `/p/` portfolio |
| `styles.css` | All CSS |
| `api/*.js` | beats, pending-beats, manage-beat, submit-beat, crate, delete-account, portfolio-meta, sitemap, track, stats, moderate-beats |
| `moderate.js` | Admin beat moderation UI (Luca only) |

**Script order:** `/app.js` → `/mypage.js` → `/portfolio.js` → `/moderate.js` → init inline in `index.html`

**Asset paths:** always root-absolute (`/styles.css`, `/app.js`, …) — relative breaks on `/p/slug` rewrites.

**No duplicate functions** across html/js modules.

## Deploy & workflow

- Repo: `sepplimc1903-design/beatswipe` · Workspace: `/Users/lucasvetina/game/beatswipe`
- Deploy: commit → push `main` → Vercel (~1–2 min). **Push only when Luca says.**
- One feature → one commit → test → next
- Never replace `index.html` via delete+upload on GitHub
- Before CSS work: `grep "accent:" styles.css` → must be `#7C3AED`
- `.env.local` never commit (`SUPABASE_SERVICE_ROLE_KEY`); `AIRTABLE_TOKEN` optional entfernen
- Local: `npx vercel dev` → `localhost:3000`, hard-refresh after edits
- Test: `beatswipe.app/p/clyriax`, My Page edit, Discover save

## Tech stack (short)

- **Beats:** Supabase `beats` — `api/beats.js`, `pending-beats.js`, `manage-beat.js`, `submit-beat.js`; Review via **Moderation UI** (`api/moderate-beats.js`, Profile → Settings) oder Supabase Table Editor
- Auth/profiles: Supabase (RLS on). Favorites: `api/crate.js` + service role
- Submit: `/api/submit-beat` → Supabase (`mypage.js`); MP3 previews: Storage bucket `beats`
- UI: English, „Favorites" not Crate, dark-only, glass cards, neutral `.wbar` waveform

## Critical pitfalls (don’t re-break)

- **Discover audio:** `renderCard` / `loadTrack` only when Discover active — not on landing (`resetDiscoverCategory()` on init)
- **Discover desktop layout:** `syncDesktopScrollMode()` — never `site-scroll` + `discover-active` together (initApp race)
- **Discover tab after reload:** always **Beats** (`full`) — don’t restore `bs_cat`
- **Swipe overflow:** both axes `visible` or clip breaks; never `overflow-x:visible` + `overflow-y:hidden`
- **Portfolio mobile:** never `pinCardForMobileDrag` on `.portfolio-surface` (ghost card)
- **Swipe perf:** `queueSaveBeatToDB` — don’t await before fly; fly before `renderCard`; no `cloneNode` on YT iframe (`buildFlyCardShell`)
- **Favorites:** `syncCrateFromDB` replace not merge; saves via `/api/crate`; no guest→DB merge on iOS session restore
- **Upload:** multi-MP3 accordion, no genre/type prefill, title from filename only
- **Preview portfolio:** `{ preview: true }` — no crate saves; Swipe-Glow muss wie Discover/`/p/` sein (`#cardGlow` + `updateGlow` — **kein** separates `bg-desktop`-Swipe-System)
- **Portfolio card-glow:** hidden in idle (`opacity: 0`); purple/orange only while swiping — never beat `--glow-color` on `/p/`
- **Local dev env:** `vercel dev` lädt `.env.local` nicht zuverlässig in APIs — `api/_env.js` liest `.env.local` als Fallback; Production nutzt Vercel `SUPABASE_SERVICE_ROLE_KEY`
- **iOS autoplay:** Playback nur in User-Geste — `unlockAudioTouch` + `onStart` (swipe) → `tryAutoplayFromGesture()`; YT via `startVideo()` (`about:blank` → `&autoplay=1`); MP3 braucht `_audioUnlocked`. YT-Overlay auf iOS bis Geste sichtbar (`isIOS()`). Kein Autoplay beim allerersten Page-Load ohne Touch (Apple-Limit). Helpers: `app.js` — `maybeAutoplayPreview`, `startVideo`, `isIOS`
- **Discover desktop layout:** `--w-discover` 480px (Card capped) — Ränder via Sidebars füllen, nicht Card auf 720px strecken. Tokens: `--discover-left`, `--sidebar-w`. Left rail `#discoverLeftRail` / `updateDiscoverLeftRail()`; `desktop-crate` ab 1100px. Swipe-overflow `visible` beibehalten.
- **YT desktop keyboard:** iframe cross-origin — `←`/`→` steuern YouTube wenn Player Fokus hat. **Nicht** `pointer-events:none` auf iframe (blockiert Scrub). Fokus-Modell: Klick auf Card → Skip/Save-Tasten; Klick in Player → Seek. `refocusSwipeShortcuts()` + `initDesktopSwipeFocus()` nach Swipe/neuer Card.
- **Laptop page body classes:** `discover-active` / `crate-active` / `mypage-active` / `profile-active` — je Screen in `applyGoTo()`; nur einer aktiv. Left rails in `index.html`. **Profile ≥1100px:** kein left rail; `.profile-layout` + Sidebar als **CSS-Grid** zentriert (`grid-template-columns: minmax(0,var(--w-page)) 300px`), ganzer Block via `page-inner--wide { max-width: min(w-page+348px,100%); margin-inline:auto }` — **nicht** Hauptspalte allein zentrieren und Sidebar daneben "mitschleppen" (alte Flex+`margin-left: calc((100vw - var(--w-page))/2)`-Formel ignorierte Sidebar-Breite, saß dauerhaft 174px zu weit rechts). Portfolio entfernt alle + `site-scroll`. Sidebars meist ab 1100px.
- **Left rails mobile:** alle vier Rails global `display: none` (`discover-`, `favorites-`, `mypage-`, `portfolio-left-rail`) — sonst Laptop-Sidebar-Text im Hintergrund sichtbar
- **Favorites Buy:** `getCrateBeats()` merged mit Live-Beats (`_beatsCache`); Buttons via `beatBuyAction()` / `resolveBeatBuyLink()` — **nicht** `d.buy.startsWith('http')` (ohne `https://` ging nicht)
- **Profile mobile scroll:** `body.profile-active #profileScreen .page-inner--wide` → `overflow-y: auto`; `.profile-wrap` / tabs `overflow: visible` — sonst kein Scroll auf Handy
- **`/p/` done screen (swipe complete):** Gäste bleiben auf `/p/`. Saves in `crate` (RAM/guest). **Desktop (≥1100px):** Liste + Preview-Split; erster Beat vorausgewählt; **Producer-CTA** in `.portfolio-done-side` unter Preview (nicht unter dem Grid). **Mobile:** nur Liste → Tap öffnet Preview (`portfolio-done-preview-open`, `backPortfolioDoneList()`); Buy **unter** Karte (`buildCratePreviewHTML(beat, { prominentBuy: true })`); Done-CTA volle Breite unter Liste. Merge `_portfolioBeats` in `getPortfolioSavedBeats()`. Replay/Guest in `.portfolio-done-foot`. **Nicht** `#portfolioSidePanel` für Done-Player.
- **Profile laptop:** zentriert (`--w-page`); Bio-Link-Sidebar rechts daneben (Flex, kein `fixed`/Overlap) ab 1100px
- **Vercel static:** kein `public/`-Ordner (sonst 404 auf `index.html` in `vercel dev`); kein `middleware.js` — Bot-OG via `vercel.json` Rewrite
- **Producer funnel:** `openProducerSignup()` in `app.js` — eingeloggt → `submitScreen`; Gast ohne Invite → Landing `#producer-start` scroll (`bs_producer_intent`); Gast mit Invite → Signup → nach `SIGNED_IN` My Page (`bs_producer_signup_intent`). Alle „Get your page“-CTAs nutzen das (Done, Footer, Side-Panel, Landing, My Page guest).
- **Producer stats:** `trackPortfolioEvent()` in `app.js`; Preview zählt nicht; View dedup `sessionStorage bs_pv_${slug}`; Buy via `data-portfolio-buy` auf `/p/`
- **QR code:** `openPortfolioQR()` in `portfolio.js` — CDN `cdn.jsdelivr.net/npm/qrcode/build/qrcode.min.js` (**nicht** `qrcode@1.5.4/build/…` — 404)
- **Moderation:** `api/moderate-beats.js` + `moderate.js`; Admin-Emails via `BEATSWIPE_ADMIN_EMAILS` (Default: `svetina.luca@gmail.com`, `hellobeatswipe@gmail.com`); Profile → Settings → Moderation
- **Legal:** Impressum + Privacy in Modals; Links auf Landing (Footer mobile+desktop), `/p/` Footer, Cookie-Banner, Profile Settings; `openLegalModal()` mit Tabs — **z-index 450** (über Cookie-Banner 350); Landing-Footer mobile: `#landScreen .site-footer` nicht global `display:none`
- **Landing hero demo:** Discover-like card (waveform + skip/save) — **keine** SKIP/SAVE-Stempel; `initHeroDemoCard()` in `app.js`. Topbar black-fade wie andere Seiten via `body:not(.portfolio-active) .bg-desktop::before` (site-scroll inkl. Landing)
- **Landing bg:** `portfolio-ambient` **nicht** global auf Laptop — nur `portfolio-active` / `/p/` boot
- **`.site-page-head` / `.land-section-head` unsichtbar am Laptop:** alte Regel `.site-page-head, .hero-visual, .land-section-head { display:none; }` stand **ohne** Media Query nach den 900px-„show"-Regeln → gewann immer (gleiche Spezifität, später im File). Fix: in `@media (max-width: 899px)` verschoben. Bei neuen globalen `display:none`-Defaults immer prüfen, ob sie **vor** den Desktop-Overrides stehen oder selbst media-gated sind. **Umkehrfall (2026-07-05):** `.faq-title-mobile` hatte eine **globale** `display:block`-Regel (id+2 Klassen, spät im File) → schlug das media-gated Desktop-`display:none` → doppelter FAQ-Titel am Laptop. Merke: „mobile-only" sichtbare Elemente ihre `display:block`-Regel **in** die Mobile-Query legen, nicht global.
- **Sidebar-Höhe uneinheitlich (Profile/Favorites vs. My Page):** Profile/Favorites rendern `.site-page-head` **außerhalb** vom Grid (Sidebar startet auf Höhe vom ersten Content), My Page rendert den Titel **innerhalb** der Hauptspalte per JS (`mypage.js`, mehrere `render...()`-Funktionen) → Sidebar sonst auf Höhe vom Titel-Text statt Content. Fix: `renderMyPageSidePanel()` misst `#myPageMain .site-page-head` Höhe zur Laufzeit und setzt sie als `margin-top` auf `#myPageSidePanel`.

## Aktuell (2026-07-05)

- **Live (frisch gepusht):** Slogan **„Your bio link. Their next beat."** — Footer, Hero-Demo, Onboarding, Meta/OG/Twitter, `portfolio.js` Fallback; Newsletter komplett entfernt (UI, Make.com-Webhook, Privacy); Profile Settings kürzer (Favorites/Legal/Account only). Davor live: Landing-/p/-Polish (FAQ, Swipe-Buttons 80px, Preview-Done-Sidebar).
- **Slogan:** Hero-H1 bleibt *Your swipe page for the bio* (Headline ≠ Slogan). **`og-image.png` + `marketing/`** noch mit altem Text — neue Assets offen.
- **Konsistenz-Pass Laptop:** App-Screens (Discover, Favorites, My Page, Profile) Apple-Button-Polish noch **nicht** — nur Landing + `/p/`.
- **Brand:** `beatswipe.net` Konkurrent; `beatswipe.com` geparkt (~2k) — Name behalten.
- **Marketing:** `marketing/` lokal/uncommitted; IG-Post mit neuem Slogan offen.

## Nächster Schritt

1. **Deploy verifizieren:** Landing Footer-Slogan, Profile Settings ohne Newsletter, `/p/clyriax` Laptop+Handy
2. **Producer outreach:** 2–3 fremde Producer — Invite-Code, `/p/clyriax` als Demo, DM-Template
3. **OG-Image + IG-Posts** mit neuem Slogan
4. **Apple-Feeling App-Screens (Laptop):** Discover, Favorites, My Page, Profile — optional vor Outreach

## Rules pflegen (Agent)

After **deploy or larger feature** (end of session if appropriate): update only **„Aktuell" + „Nächster Schritt"** here (~5–15 lines). No per-fix changelog. New hard-won lesson → one bullet under pitfalls; big history → `@beatswipe-archive.mdc` only when asked.

## Design

- `--accent: #7C3AED`, `--accent-mid: #A78BFA`
- Anim: `cubic-bezier(0.22,1,0.36,1)`; respect `prefers-reduced-motion`
- Toast not `alert()` · `confirm()` ok for destructive actions

## Invite codes

`BEATSWIPE25`, `beatswipe` — gate in `goTo()`, don’t delete (comment to pause)

Kontakt: hellobeatswipe@gmail.com · @beatswipe.app
